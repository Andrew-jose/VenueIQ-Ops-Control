# Example terraform.tfvars — copy to terraform.tfvars and fill in real values.
# NEVER commit terraform.tfvars to version control.

project_id             = "your-gcp-project-id"
region                 = "us-central1"
artifact_registry_repo = "venueiq-repo"
